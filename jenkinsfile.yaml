pipeline{
    agent any
    stages {
        stage('Hello'){
            steps{
                echo "Hello world"
                echo "$env.workspace"
                bat 'runtests.bat'
            }
         }
    }
  }
